Tech Stack

Backend: Django + Django REST Framework
Frontend: React (with React Router, Axios, Tailwind CSS)
Database: MySQL
Auth: JWT (djangorestframework-simplejwt)


Project Structure
restaurant-app/
├── backend/          # Django project
│   ├── accounts/     # User auth & roles
│   ├── menu/         # Food items, categories
│   ├── orders/       # Order management
│   └── restaurant/   # Settings, config
└── frontend/         # React app
    ├── src/
    │   ├── pages/
    │   ├── components/
    │   ├── context/
    │   └── api/

User Roles & Access
RoleAccessSuper AdminManage admins, all restaurants, full system controlAdminManage menu, view/manage orders, manage usersUserBrowse menu, place orders, view order history

Backend (Django) — What to Build
1. MySQL Setup (settings.py)
pythonDATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'restaurant_db',
        'USER': 'root',
        'PASSWORD': 'yourpassword',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
2. Custom User Model (accounts/models.py)
pythonclass CustomUser(AbstractUser):
    ROLE_CHOICES = [
        ('superadmin', 'Super Admin'),
        ('admin', 'Admin'),
        ('user', 'User'),
    ]
    role = models.CharField(max_length=20, choices=ROLE_CHOICES, default='user')
    phone = models.CharField(max_length=15, blank=True)
    profile_image = models.ImageField(upload_to='profiles/', blank=True)
3. Menu Models (menu/models.py)
pythonclass Category(models.Model):
    name = models.CharField(max_length=100)
    image = models.ImageField(upload_to='categories/')

class FoodItem(models.Model):
    VEG = 'veg'
    NONVEG = 'nonveg'
    TYPE_CHOICES = [(VEG, 'Veg'), (NONVEG, 'Non-Veg')]

    category = models.ForeignKey(Category, on_delete=models.CASCADE)
    name = models.CharField(max_length=200)
    description = models.TextField()
    price = models.DecimalField(max_digits=8, decimal_places=2)
    image = models.ImageField(upload_to='food/')
    food_type = models.CharField(max_length=10, choices=TYPE_CHOICES)
    is_available = models.BooleanField(default=True)
    rating = models.FloatField(default=0.0)
4. Order Models (orders/models.py)
pythonclass Order(models.Model):
    STATUS = [('pending','Pending'),('confirmed','Confirmed'),
              ('preparing','Preparing'),('delivered','Delivered'),('cancelled','Cancelled')]
    user = models.ForeignKey(CustomUser, on_delete=models.CASCADE)
    status = models.CharField(max_length=20, choices=STATUS, default='pending')
    total_price = models.DecimalField(max_digits=10, decimal_places=2)
    created_at = models.DateTimeField(auto_now_add=True)
    address = models.TextField()

class OrderItem(models.Model):
    order = models.ForeignKey(Order, related_name='items', on_delete=models.CASCADE)
    food_item = models.ForeignKey(FoodItem, on_delete=models.CASCADE)
    quantity = models.PositiveIntegerField()
    price = models.DecimalField(max_digits=8, decimal_places=2)
5. API Endpoints (DRF)
POST   /api/auth/register/
POST   /api/auth/login/          → returns JWT tokens
POST   /api/auth/refresh/
GET    /api/auth/me/

GET    /api/categories/
GET    /api/menu/                → all food items
GET    /api/menu/<id>/
POST   /api/menu/               → admin only
PUT    /api/menu/<id>/          → admin only
DELETE /api/menu/<id>/          → admin only

POST   /api/orders/             → place order
GET    /api/orders/             → user's own orders
GET    /api/orders/all/         → admin: all orders
PATCH  /api/orders/<id>/status/ → admin: update status

GET    /api/admin/users/        → superadmin only
PATCH  /api/admin/users/<id>/role/ → superadmin only
6. Permissions
pythonclass IsSuperAdmin(BasePermission):
    def has_permission(self, request, view):
        return request.user.role == 'superadmin'

class IsAdminOrSuperAdmin(BasePermission):
    def has_permission(self, request, view):
        return request.user.role in ['admin', 'superadmin']

Frontend (React) — What to Build
Pages:

/login — Login form (all roles)
/register — User registration
/ — Home: hero, categories, featured items
/menu — Full menu with filters (category, veg/nonveg, price)
/cart — Cart page with quantity controls
/checkout — Address + place order
/orders — User order history with status
/admin/dashboard — Admin: stats cards
/admin/menu — Admin: add/edit/delete food items
/admin/orders — Admin: view & update order status
/superadmin/users — Super Admin: manage all users & roles

Key React Setup:
npm create vite@latest frontend -- --template react
npm install axios react-router-dom tailwindcss @tailwindcss/forms
npm install react-hot-toast react-icons
Auth Context (context/AuthContext.jsx)
jsx// Store JWT token, user role in context
// On login: save token to localStorage
// Axios interceptor: attach Bearer token to all requests
// Role-based redirect after login:
//   superadmin → /superadmin/dashboard
//   admin      → /admin/dashboard
//   user       → /
Protected Route Component
jsx<ProtectedRoute allowedRoles={['admin', 'superadmin']}>
  <AdminDashboard />
</ProtectedRoute>

MySQL Database Setup
sqlCREATE DATABASE restaurant_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'restaurant_user'@'localhost' IDENTIFIED BY 'yourpassword';
GRANT ALL PRIVILEGES ON restaurant_db.* TO 'restaurant_user'@'localhost';
FLUSH PRIVILEGES;

Installation Commands
Backend:
bashpip install django djangorestframework djangorestframework-simplejwt
pip install mysqlclient django-cors-headers Pillow
django-admin startproject restaurant .
python manage.py startapp accounts
python manage.py startapp menu
python manage.py startapp orders
python manage.py makemigrations && python manage.py migrate
python manage.py createsuperuser
Frontend:
bashnpm create vite@latest frontend -- --template react
cd frontend
npm install axios react-router-dom react-hot-toast react-icons
npx tailwindcss init
npm run dev

Summary of Logins
RoleCan DoSuper AdminCreate/manage admins, view all data, change user rolesAdminAdd/edit/delete menu items, manage orders, view usersUserRegister, browse menu, add to cart, place & track orders

You can copy this entire prompt and paste it to Claude with the message:

"Build this full-stack restaurant food app step by step, starting with the Django backend."